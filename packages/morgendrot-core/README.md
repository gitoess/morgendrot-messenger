# `@morgendrot/core`

TypeScript-Kern für Messenger (Offline-Mailbox-Queue, später IOTA) — **ohne** React/Next, **ohne** feste `localStorage`/`fetch`-Bindung.

- **Plan:** `docs/MORGENDROT-CORE-PACKAGE-PLAN.md`
- **Tests:** `npm run test:unit` in diesem Ordner
