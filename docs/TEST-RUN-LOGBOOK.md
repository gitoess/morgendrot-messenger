# Test-Lauflog (manuell gepflegt)

**Zweck:** Nachvollziehbare **Ist-Läufe** von Smoke-, Frontend-, Core- und Realworld-Kommandos — **kein** Ersatz für CI; dient **Boss/Release** und **Handy-Entscheid** (**`docs/HANDY-TEST-WINDOW.md`**).

**Regel:** Nach größeren Messenger-/Core-Änderungen oder vor **Messenger-Handy-Feldtest** eine Zeile ergänzen — idealerweise **`test:messages*`** getrennt von Ticket-Läufen (**`test:tickets-accesskey-realworld`** / Alias **`test:realworld`**), weil letztere **eine andere Kachel** sind und **nicht** zum Messenger-Smoke gehören.

---

## Einträge

| Datum | Umgebung | Kommandos / Notiz | Ergebnis |
|--------|-----------|---------------------|----------|
| **2026-06-02** | Repo lokal (Windows), **Standalone: Event-Inbox + Purge Direkt-RPC** | `fetchMessagingEventInboxRpcRows`, `purge-handshake-hybrid`, `buildPurgeHandshakeTransaction`; `npm run test:h15-direct-submit` | **OK (Code)** — Event-Union im Direkt-Posteingang; Handshake-Purge ohne `/purge-handshake` in Standalone. |
| **2026-06-02** | Repo lokal (Windows), **§ H.6f Android FG-Service (minimal)** | `MessengerForegroundService` + `MessengerFgSyncPlugin`; `capacitor-foreground-sync.ts`; `npx vitest run frontend/lib/capacitor-foreground-sync.test.ts`; `frontend/android` → `gradlew assembleDebug` | **OK (Code)** — APK-Feldtest: Notification sichtbar, Akku &lt;15 % ohne Laden startet nicht; Opt-in in Basis-URL-Karte. |
| **2026-06-02** | Repo lokal (Windows), **§ H.25a Schreibtisch-Nachzug** | `cd frontend` → `npm run test:h25a-lora-image` (NAK-Nachsenden, >32 Segmente abgelehnt, INIT+SEG) | **OK (Code)** — Feldtest 2 Heltecs weiter offen (`TESTING.md` § H.25a). |
| **2026-06-02** | Repo lokal (Windows), **§ H.15 UI-Sync + Chunk-Recovery-Test** | `direct-iota-ui-events.ts`; `notifyDirectIotaUiChanged` bei Ketten-IDs/Flags; `getDirectIotaHeaderStatusLine`; `chunk-load-error.test.ts`; `npm run test:h15-direct-submit` | **OK (Code)** — Chat-Kopf/Offline zeigen Direkt-Lücken auch ohne Autarkie; Blur/Toggle aktualisiert Kopf im selben Tab. |
| **2026-06-02** | Repo lokal (Windows), **§ H.15 Autarkie-Bootstrap + Checkliste + H.3o.6 (3)** | `getDirectChainIdsReadiness`, `persistDirectChainFieldIds`, `getAutarkyChecklistItems`, `getAutarkyStatusLine` (Offline-Karte); `composer-encryption-context-hint`; `npm run test:h15-direct-submit` | **OK (Code)** — Direkt-Posteingang ohne vollständigen API-Snapshot; Autarkie-Checkliste an Readiness; Composer-Hinweis Funk-PSK vs. Online-Schloss. Feldtests weiter offen. |
| **2026-06-02** | Repo lokal (Windows), **H.3o.6 Schritt 1-2 (Meshtastic channelIndex + Gruppen-Secondary-Metadaten)** | `cd frontend` → `npx vitest run frontend/lib/meshtastic-channel-index.test.ts frontend/components/chat-view-transport-card.test.tsx` ; `npx vitest run frontend/components/chat-view-send-panel.test.tsx frontend/lib/meshtastic-channel-index.test.ts` ; `npx vitest run frontend/lib/messenger-group-store.test.ts frontend/lib/group-mailbox-pairwise-send.test.ts frontend/components/chat-view-send-panel.test.tsx` | **OK (Code)** — Guardrails `0..7`, interner Kanalindex-State + Expert-Input, Gruppen-Metadaten (`channelIndex/channelName/pskRef`) grün. **Offen:** H.3o.6 (4) manueller Feldtest mit random PSK (siehe Roadmap-Checkliste). |
| **2026-05-28** | Repo lokal (Windows), **§ H.15 Connect clientseitig** | `direct-iota-connect` + `connect-hybrid`; Partner-Connect offline | **OK (Code)** |
| **2026-05-28** | Repo lokal (Windows), **§ H.15 Handshake senden clientseitig** | `handshake-ecdh-txb` + `handshake-send-hybrid`; Hybrid vor API | **OK (Code)** |
| **2026-05-28** | Repo lokal (Windows), **§ H.16 Peering-QR** | `peering-qr` mp/mc+e; Puls + Partner-Panel; Capacitor-Scan | **OK (Code)** |
| **2026-05-28** | Repo lokal (Windows), **§ H.15 B.2 on-chain Handshake-RPC** | `handshake-offers-rpc` in core; `fetchHandshakeOffers`/`findPeerHandshake` RPC-first; Poll bei Basis-Ausfall | **OK (Code)** |
| **2026-05-28** | Repo lokal (Windows), **§ H.15 B.2 Peering offline** | connected-peers + handshake-offers Cache; Puls Peering; Basis-Guards; Tests | **OK (Code)** |
| **2026-05-28** | Repo lokal (Windows), **§ H.15 B.1 private Inbox-RPC + Cache** | `mailboxObjectId` in `tryFetchDirectMailboxInboxViaIota`; `loadedViaRpc` + Inbox-Cache `liveSource`; UI-Banner | **OK (Code)** |
| **2026-05-28** | Repo lokal (Windows), **§ H.15 B.1 Inbox-RPC + Chat-Snapshot** | `fetchInboxFromAllOwnedMailboxes` RPC-first; Chat „IDs-Snapshot veraltet“; `inbox-multi-mailbox-fetch.test` | **OK (Code)** |
| **2026-05-28** | Repo lokal (Windows), **§ H.15 B.1 Snapshot-TTL (Puls)** | `getDirectChainSnapshotMeta`, `savedAtMs` bei Speichern, Puls-Zeile (30-Min wie Offline-Cache); `test:h15-direct-submit` (18) | **OK (Code)** |
| **2026-05-28** | Repo lokal (Windows), **§ H.15 Phase 2 Fortsetzung (Code)** | Chat-Kopf `ChatViewDirectIotaPathBadge`, Puls `listDirectIotaSetupGaps`; `test:h15-direct-submit` (13) | **OK (Code)** — manuelle § 2 in `HANDY-LATER-MANUAL-TESTS.md` § D. |
| **2026-05-28** | Repo lokal (Windows), **§ H.15 Phase 2 (Code)** | `direct-iota-error-messages.ts`, Hybrid-Fehler-Merge in `mailbox-send-hybrid.ts`, Pfad-UI Basis-offline; `npm run test:h15-direct-submit` erweitert | **OK (Code)** — § 2 Smoke weiter in `HANDY-LATER-MANUAL-TESTS.md` § D. |
| **2026-05-28** | Repo lokal (Windows), **Roadmap: manuelle Runde vorgemerkt + Chunk-Recovery global** | `docs/HANDY-LATER-MANUAL-TESTS.md`; `ChunkLoadRecovery` + `chunk-load-error.ts`; Einstellungen Status-Poll 10 s; Vitest `chunk-load-error.test.ts` (3) | **OK (Code)** — § 9.6/§ 10/Handoff/H.15 § 2 in späterer manueller Runde (**HANDY-LATER-MANUAL-TESTS**). |
| **2026-05-28** | Repo lokal (Windows), **Roadmap Schreibtisch-Ritual** | `npm run test:smoke` (41/41), `npm run test:h15-direct-submit` (5), `npm run test:frontend-unit` (560) nach Handoff-Reconnect-Scheibe | **OK** — automatisierte Mindestabdeckung grün; § 9.6 Queue + § 10 APK + H.15 § 2 weiter manuell. |
| **2026-05-28** | Repo lokal (Windows), **Roadmap: Handoff Basis-Apply nach Reconnect** | `handoff-pending-server-apply.ts`, HandoffImportPanel Banner + Draft-Restore bei `backendOnline`; `npm run test:frontend-unit` | **OK (Code)** — manuell: lokal vormerken → Basis trennen → Reconnect → Import bestätigen. |
| **2026-05-28** | Repo lokal (Windows), **Roadmap: API_BIND_HOST + APK-LAN** | `API_BIND_HOST=0.0.0.0` bei `dev:lan`; CORS `capacitor://`; Offline-Status mit Probe-Feedback | **OK** — APK erreicht PC nur mit LAN-IP + `dev:lan`, nicht `127.0.0.1`. |
| **2026-05-28** | Repo lokal (Windows), **Roadmap: APK-API-URL + Queue/Reconnect-Scheibe** | `getApiBase()`/Override, `CapacitorApiBaseCard`, Queue-Opt-in-Button, Reconnect-Drain, § 10-Doku; `npm run test:frontend-unit` | **OK** — Scheiben 2–4 der Betriebsliste umgesetzt; § 9.6/§ 10 Gerät manuell offen. |
| **2026-05-28** | Repo lokal (Windows), **Roadmap: dev:stop + Handoff-Reconnect-Hinweis** | `npm run dev:stop` (Script `scripts/dev-stop.mjs`); Reconnect-Toast bei lokalem Handoff; Fahrplan A–C abgeschlossen, nächste Scheiben § 9.6/§ 10/APK-Gerät; `npm run test:frontend-unit` | **OK** — Port-Kollisionen reproduzierbar bereinigen; Nutzerführung bei Basis-Wiederkehr. |
| **2026-05-28** | Repo lokal (Windows), **Chunk-Load-Härtung bei Offline/Refresh** | Dev-Schutz: `PwaServiceWorkerRegister` entfernt alte SW-Registrierungen in `NODE_ENV!=production`; Error-Page erkennt Chunk-Load-Fehler und bietet Hard-Reload-Button; danach `npm run test:frontend-unit` | **OK** — reduziert „Loading chunk ... failed“ im Dev-/Recovery-Pfad, inkl. klarer Nutzeraktion bei Restfehlern. |
| **2026-05-28** | Repo lokal (Windows), **Hydration-Härtung Offline-Status (global)** | `useOfflineStatus` auf hydration-sichere Initialwerte umgestellt (Inbox-Cache/Queue erst nach Mount aus `localStorage`), danach `npm run test:frontend-unit` | **OK** — SSR/Client-Mismatches im Offline-Statusblock strukturell reduziert; Unitlauf grün. |
| **2026-05-28** | Repo lokal (Windows), **Roadmap-Fortsetzung: Queue-Testbarkeit im Betriebsritual** | UI-Hinweis ergänzt (Queue-Opt-in Command in Offline-Statuskarte) + §9 Schritt 6 in `HANDY-FIRST...` um reproduzierbare Queue-Vorbereitung erweitert | **OK (Doku/UI):** Nächster Betriebslauf kann Queue-Konsistenz gezielt auf PASS/FAIL statt N/A prüfen. |
| **2026-05-28** | Messenger-Betriebslauf (geführter Check §9 Offline->Reconnect) | Manuell: Schritt 1 (Online-Start) **PASS**, Schritt 2 (Cache-/Offline-Hinweis) **PASS**, Schritt 3 nach Hydration-Fix **PASS** (kein Fehler, Reconnect/Refresh ok), Schritt 4 Queue-Konsistenz **N/A** (keine Queue-Einträge) | **OK** — Betriebsritual aktuell grün; Queue-Punkt bei nächstem Lauf mit wartenden Sends gezielt nachziehen. |
| **2026-05-28** | Repo lokal (Windows), **Hydration-Fix OfflineStatusCard (Queue-Zeile)** | Zweiter SSR/Client-Mismatch in Queue-Zeile behoben (`Opt-in aus` erst nach Hydration anzeigen), danach `npm run test:frontend-unit` | **OK** — Recoverable Hydration Error bei Schritt 3 beseitigt; Unitlauf grün. |
| **2026-05-28** | Repo lokal (Windows), **Hydration-Fix OfflineStatusCard** | Frontend: SSR/Client-Mismatch in `offline-status-card.tsx` behoben (`hydrated`-Gate für „vor X Min.“), danach `npm run test:frontend-unit` | **OK** — Recoverable Hydration Error beseitigt; Unitlauf grün. |
| **2026-05-28** | Repo lokal (Windows), **Roadmap-Fortsetzung: APK-Pipeline als One-Command** | `cd frontend` -> `npm run apk:debug:build` (enthaelt `build:capacitor-web` + `cap:sync:android` + `gradlew assembleDebug`) | **OK** — Pipeline laeuft durch und erzeugt den reproduzierbaren APK-Basislauf ohne manuelle Einzelschritte. |
| **2026-05-28** | Repo lokal (Windows), **Roadmap-Fortsetzung: ActiveProfilePanel Lokal-Hinweis** | Frontend: `active-profile-panel.tsx` zeigt Warnhinweis, wenn `status.fromLocalHandoff===true`; `npm run test:frontend-unit` | **OK** — lokal vorgemerkte Profile sind jetzt direkt im Profilpanel sichtbar, nicht nur in der Offline-Statuskarte. |
| **2026-05-28** | Repo lokal (Windows), **Roadmap-Fortsetzung: Handoff-Lokalmodus sichtbar** | Frontend: `useOfflineStatus` + `OfflineStatusCard` um Kennzeichen „nur lokal vorgemerkt“ erweitert; `npm run test:frontend-unit` | **OK** — UI zeigt klaren Hinweis, wenn Basis-Apply noch aussteht; Frontend-Unitlauf grün. |
| **2026-05-28** | Repo lokal (Windows), **Capacitor-WebDir produktiviert (Next-Export)** | `cd frontend` -> `npm run build:capacitor-web` (Output `out/`), `npm run cap:sync:android`; `cd android` -> `gradlew assembleDebug` | **OK** — APK-Basis nutzt jetzt Next-Export statt Minimal-Shell; Android-Debug-Build weiterhin erfolgreich. |
| **2026-05-28** | Repo lokal (Windows), **Mini-Meilenstein C (Handoff-Apply-Entkopplung, gezielt)** | Frontend: lokaler Handoff-Apply-Fallback ergänzt (`handoff-local-apply.ts`), Button „Lokal vormerken (ohne Basis)“, Status-Fallback aus lokal vorgemerktem Handoff in `fetchStatus` | **OK (Code):** Bei Basis-Ausfall bleibt ein lokal vorgemerktes Profil sichtbar (Role/Profile/Transport/Label). Endgültiges Apply weiterhin bewusst über Basis-API. |
| **2026-05-28** | Repo lokal (Windows), **Capacitor-Toolchain vervollstaendigt** | JDK 21 (Temurin) installiert, Android SDK (`platform-tools`, `build-tools;36.0.0`, `platforms;android-36`) installiert; `frontend/android/local.properties` gesetzt; `cd frontend/android` -> `gradlew assembleDebug` | **OK** — Android-Debug-Build erfolgreich (`BUILD SUCCESSFUL`), vorherige `JAVA_HOME`/SDK-Blocker geloest. |
| **2026-05-28** | Repo lokal (Windows), **Capacitor-Basis (Mini-Meilenstein A)** | `cd frontend` -> `npm install -D @capacitor/cli`, `npm install @capacitor/android`, `npm run cap:doctor`, `npm run cap:sync:android`; `cd android` -> `gradlew assembleDebug` | **Teil-OK:** Native Basis aktiv (`capacitor.config.ts`, `android/`, Sync gruen, MLKit-Plugin eingebunden). **Offener Blocker:** Gradle-Build stoppt lokal mit `JAVA_HOME is not set` (JDK/Toolchain fehlt auf Host). |
| **2026-05-28** | Repo lokal (Windows), Offline-Phase-1-Scheiben (Status/Kontakte/Inbox/Queue/Handoff-Import) | Root: **`npm run test:frontend-unit`** nach sukzessiven Änderungen (Status-Cache TTL + Banner, Kontakt-Cache TTL, Inbox-Cache pro Kontext + Offline-Banner, Queue-Status `queued/retrying/backoff`, Handoff lokale Vorschau + Draft-Restore + Draft-TTL 24h) | **OK** — Frontend Vitest durchgehend grün (**114 Dateien / 554 Tests**). |
| **2026-05-28** | Repo lokal (Windows), Fahrplan-Fortsetzung **§ H.25a / § H.15 Stufe 2** (Schreibtisch) | Root: **`npm run test:smoke`** (41/41 Modulgruppen), **`npm run test:h15-direct-submit`** (Vitest `direct-iota-plain-submit.test.ts`, 5 Tests) | **OK** — Schreibtisch grün; **H.25a Feldtest mit zwei Heltecs** bleibt offen (nur manuell). |
| **2026-05-20** | Repo lokal (Windows), § H.22 **M4d** Deploy + Live | **`npm run deploy:move-package`** → `PACKAGE_ID=0x014ef8…a1c7`; API **`/create-private-mailbox`** on-chain OK (Object `0xa41455…055d`, Digest `6P14hcj…`); UI-Button **„Eigene private Mailbox erstellen“** | **OK** — Shared **`MAILBOX_ID`** ggf. separat via `create_globals` für neues Paket. |
| **2026-05-20** | Repo lokal (Windows), § H.22 **M4d** `create_private_mailbox` (Implementierung) | Move (`PrivateMailbox`, `create_private_mailbox`, `store_*_private`); API **`/create-private-mailbox`**; UI (Puls + Identität); Direct-IOTA `*_private` bei Kontakt-Mailbox-Override; Root **`npm run test:smoke`** (40/40), **`test:core`**, **`test:frontend-unit`** (83/442) | **OK** |
| **2026-05-20** | Repo lokal (Windows), § H.4 Schreibtisch-Ritual (Phase A) | Root: **`npx tsc --noEmit`** (grün); **`frontend`**: **`npx tsc --noEmit`**, **`npm run lint`** (3 Warnungen, 0 Fehler), **`npm run check:circular`**, **`npm run test:frontend-unit`** (**83** Dateien / **442** Tests); Root: **`npm run test:core`** (17/61), **`npm run test:h15-direct-submit`** (5), **`npm run test:smoke`** (40/40), **`npm run check:pwa-desk`** (A+B OK). Fix: TS in Frontend (Telegram-Transport, `appendMeshMessage`, Tangle-Import, `resolveForceLegacyEncrypted` Default mailbox). | **OK** — Schritt 1 erfüllt; **`check:pwa-desk:full`** + Gerät L1–L5 optional als Nächstes. |
| **2026-05-17** | Repo lokal (Windows), § H.26 Telegram + Posteingang/Verankern | Root: **`npm run test:frontend-unit`**; geändert: Telegram-API/UI, Posteingang-Toolbar, Protokoll-Chunks, `mailbox-inbox-page-fetch`, morg-pkg `commandPlaintext`, Messaging-Event-Default | **OK** — Next **200** auf `:3341` nach Syntaxfix; **82 Dateien / 439 Tests** grün. |
| **2026-04-22** | Repo lokal (Windows), Meshtastic **Port 7** + Posteingang + Doku | Root: **`npm run test:frontend-unit`** nach Unishox-Dekodierung komprimierter LongFast-Texte (`onMeshPacket`), Inbox-Partnerfilter für alle eingehenden Mesh-Zeilen, Setup-Hinweise (Events/RX/F12-Debug); README/CHANGELOG/Heltec/LORA-Smoke/Meshtastic-Baukasten aktualisiert | **OK** — Suite gruen (**75 Dateien / 421 Tests**). |
| **2026-04-22** | Repo lokal (Windows), R1-Dialog vereinfacht (kein Doppelpfad) | Root: **`npm run test:frontend-unit`** nach Umbau auf Hauptaktion „Zu LoRa senden“, Auto-Feldübernahme bei Import, und Expertenaktionen hinter optionalem Toggle | **OK** — Suite gruen (**75 Dateien / 420 Tests**), Lint in geaenderten Dateien ohne Befund. |
| **2026-04-22** | Repo lokal (Windows), R1 Relay-Protokollierung + README-Abgleich | Root: **`npm run test:frontend-unit`** nach Relayer-Submit-Protokoll (`submitted/reject/error`, Fehlercode/Notiz) in R1-Beta-Dialog und Doku-Update (`README`, Changelog, Roadmap) | **OK** — Suite gruen (**75 Dateien / 420 Tests**), geaenderte Dateien lint-frei. |
| **2026-04-22** | Repo lokal (Windows), Import/Export + R1 Rohdaten-Submit Beta | Root: **`npm run test:frontend-unit`** nach Umbenennung Export -> Import/Export und neuem R1-Dialog (Envelope-Validierung, lokale Queue, manuelles `anchored` mit Digest-Übernahme) | **OK** — Suite gruen (**75 Dateien / 420 Tests**), keine neuen Lint-Fehler in geaenderten Dateien. |
| **2026-04-22** | Repo lokal (Windows), Inventory Folge-Iteration (Paging/Filter/Tresor-Digest) | Root: **`npm run test:frontend-unit`** nach Recovery-Paging+Retry, Filter (Typ/Status/Recovery) und Tresor-Sync fuer Digests (`/api/vault-personal-secrets`, Auto-Speichern optional) | **OK** — Suite gruen (**75 Dateien / 420 Tests**), geaenderte Dateien lint-frei. |
| **2026-04-22** | Repo lokal (Windows), Verankerungs-Liste (Inventory) + Recovery-Button | Root: **`npm run test:frontend-unit`** nach neuer Inventory-UI (Export -> Verankerungen), LocalStorage-Sammelstelle fuer `txDigest`-Quittungen und Direct-RPC-Recovery pro Eintrag | **OK** — Suite gruen (**75 Dateien / 420 Tests**), keine Lint-Fehler in den geaenderten Dateien. |
| **2026-04-22** | Repo lokal (Windows), Inbox-Toolbar UX-Runde | Root: **`npm run test:frontend-unit`** nach Umbau „Protokoll auf Chain verankern“ in Export-Menü, Toggle-Filterleiste (Posteingang/Kanal/Partner), und Ausblenden/Wiedereinblenden-Button | **OK** — neue Inbox-Bedienlogik stabil; Suite bleibt grün (**75 Dateien / 420 Tests**). |
| **2026-04-22** | Repo lokal (Windows), Messenger-Inbox entrümpeln (Boss-UI raus) | Root: **`npm run test:frontend-unit`** nach Entfernen von Boss-Übersicht-Toggle + eingebettetem Einsatz-Profil/Kontakte-Block im Posteingang | **OK** — Messenger-Inbox vereinfacht; Suite weiter grün (**75 Dateien / 420 Tests**). |
| **2026-04-22** | Repo lokal (Windows), **§ H.0 #4** Recovery direkt nach Unlock | **`cd frontend`**: **`npx vitest run frontend/lib/unlock-recovery-flow.test.ts frontend/hooks/use-chat-view-connection-actions.test.ts`**, danach Root **`npm run test:frontend-unit`** | **OK** — Unlock-Recovery-Offer helper + Handshake-Autofill-Hook grün; Gesamtsuite **75 Dateien / 420 Tests**. |
| **2026-04-22** | Repo lokal (Windows), IDs-/Direkt-RPC-Aufräumen + Handshake-Autofill | **`cd frontend`**: **`npx vitest run frontend/hooks/use-chat-view-connection-actions.test.ts`** (neu: Handshake->Peer-Pub-Autofill + Nicht-0x-Guard), danach Root **`npm run test:frontend-unit`** | **OK** — gezielte H.1a/H.1b-Scheibe grün; Gesamtsuite **74 Dateien / 417 Tests**. |
| **2026-04-22** | Repo lokal (Windows), **§ H.1a/H.1b** kleine PC-Iteration (Posteingangszähler) | **`cd frontend`**: **`npx vitest run frontend/components/chat-view-inbox-toolbar.test.tsx`** (neu: 3 Tests Badge „gesamt“ + Hinweis „sichtbar“) und danach Root **`npm run test:frontend-unit`** | **OK** — gezielte Regression gegen „nur sichtbare Nachrichten gezählt“; 1 gezieltes RTL-File + voller Frontend-Unitlauf (**72/412**) grün. |
| **2026-04-22** | Repo lokal (Windows), **§ H.1a** Folge-Scheibe nach H.2-Schreibtischlauf | Root: **`npm run test:frontend-unit`** → `npm run test:unit --prefix frontend` (Vitest) | **OK** — **72** Dateien, **412** Tests grün; Basis für weitere kleine H.1a/H.1b-Slices stabil. |
| **2026-04-22** | Repo lokal (Windows), Fahrplan **§ C.0b / § H.2** Schreibtisch-Ritual nach Vault-/Messenger-UI-Update | Root: **`npm run check:pwa-desk:full`** (A: `build:pwa-icons`, B: `sync:handbook` = 15 Dateien, C: `frontend build` Next 16.1.6; 7 statische Routes; `[check-pwa-manual-desk] OK`) | **OK** — Schreibtisch-Teil erfüllt; nächster Schritt laut Fahrplan: Geräte-Checks **L1–L5** aus **`docs/PWA-MANUAL-CHECKS.md`** und danach nur bei Bedarf weitere H.1a/H.1b-Scheiben. |
| **2026-04-21** | Handy-/Messenger-Manualtest, Einmaltest-Matrix (Single Messenger + 2 Heltecs) | Manuell je Funktion genau 1x: encrypted-online Send, Pfad-4 offline->online Recovery, Queue-Einzellöschung, Protokoll-Dialog „Vollbericht zu groß“ (Schrittführung), Outbox-Transportlabel mesh+online/mailbox | **OK** — alle geprüften Funktionen einmalig erfolgreich; Bilddatei-Funkpfad bleibt bewusst Backlog. |
| **2026-04-21** | Handy-/Messenger-Manualtest, privater Chat (LoRa Pfad 4 + encrypted online) | Manuell: Pfad 4 Text ueber Funk gesendet, Internet/Basis kurz getrennt, nach Netzrueckkehr Mailbox/IOTA-Drain beobachtet; danach encrypted-Online im gleichen Recovery-Ablauf getestet | **OK** — LoRa-Sendung blieb funktionsfaehig; Mirror/Drain lief nach Reconnect; encrypted online im Anschluss erfolgreich. |
| **2026-04-21** | Repo lokal (Windows), Stage-2-Vorprüfung vor Handy-Run (H.15) | Root: **`npm run test:smoke`** (36/36), **`npm run test:h15-direct-submit`** (1 Datei, 5 Tests); zusätzlich wegen Frontend-Änderungen **`npm run test:frontend-unit`** (71 Dateien, 406 Tests) | **OK** — Vorprüfung gemäß **`docs/HANDY-FIRST-STAGE2-CLIENT-SUBMIT-SMOKE.md`** § 5 erfüllt; manuelle Stage-2-Checks (§ 2, Puls/Signer/Basis stop-start) bleiben separat am Gerät/Browser. |
| **2026-03-28** | Repo lokal (Windows), **§ H.1a** morg-pkg / Inbox-Partner / Workspace-Storage | **`cd frontend`**: **`npm run test:unit`** — **66** Dateien, **370** Tests | **OK**. |
| **2026-03-28** | Repo lokal (Windows), **§ H.1a** slide/mesh-QR + IOTA-Dashboard-RTL | **`cd frontend`**: **`npm run test:unit`** — **63** Dateien, **341** Tests | **OK**. |
| **2026-03-28** | Repo lokal (Windows), **§ H.1a** Transport/Wald/Mesh/Kamera | **`cd frontend`**: **`npm run test:unit`** — **60** Dateien, **323** Tests (**`chat-view-messenger-transport`**, **`chat-wald-connection`**, **`mesh-contact-verify`**, **`device-detect`**) | **OK**. |
| **2026-03-28** | Repo lokal (Windows), **§ H.1a** Sendepfad + PWA-Dashboard-RTL | **`cd frontend`**: **`npm run test:unit`** — **56** Dateien, **311** Tests (**`chat-view-send-utils.test.ts`**, **`dashboard-pwa-install-card.test.tsx`**) | **OK**. |
| **2026-03-28** | Repo lokal (Windows), **§ H.1a** weitere Scheiben | **`cd frontend`**: **`npm run test:unit`** — **54** Dateien, **299** Tests (neu: **`package-id-compare`**, **`morg-emergency-v1-text`**, **`inbox-load-error`**) | **OK**. |
| **2026-03-28** | Repo lokal (Windows), **§ H.1a**-Scheibe | **`cd frontend`**: **`npm run test:unit`** — **51** Dateien, **276** Tests (neu: **`format-unknown-error.test.ts`**) | **OK** — weiter **L1–L5** / **H.15 Stufe 2** manuell. |
| **2026-03-28** | Repo lokal (Windows), Fortsetzung **§ H.2** nach Roadmap | **`npm run check:pwa-desk:full`** — Next Production-Build (7 statische Routes, „Skipping validation of types“ laut Projektsetup), **`[check-pwa-manual-desk] OK`** | **OK** — Schreibtisch-Pflicht **§ C.0b Stufe 2** / **`docs/PWA-MANUAL-CHECKS.md`**; **als Nächstes** **§ H.1a**, Gerät **L1–L5**, dann **§ H.15 Stufe 2** manuell (**`HANDY-FIRST-STAGE2-…`**). |
| **2026-03-28** | Repo lokal (Windows), Tranche **Mailbox-Persistent**, **§ H.3n**-Vitest, **LoRa robust**, **PWA full** | Root **`npm run test:smoke`** (36/36); **`npm run test:h15-direct-submit`**; **`npm run check:pwa-desk:full`** (A+B+C, Next **16.1.6**); **`cd frontend`**: **`npm run test:unit`** (50 Dateien, 266 Tests) | **OK** — **`docs/MESSAGING-MAILBOX-SSOT-SPEC.md`**, **`messagingPersistenceMode`** / **`execute-command`**, SOS **`morg-sos-*-retry/ack`**, **`prepareImageForLoRaRobust`**; **`CHANGELOG`**, **`ROADMAP`** (H.15-Stufe-2 hinten); siehe **`docs/PWA-MANUAL-CHECKS.md`**. |
| **2026-03-28** | Repo lokal (Windows), **§ H.15 Stufe 2** — Merge-Ritual **`TESTING.md`** + Smoke-Doku **`HANDY-FIRST-STAGE2-CLIENT-SUBMIT-SMOKE.md`** § 0/§ 4 (automatisiert) | Root **`npx tsc --noEmit`** (TS6059 `rootDir` vs. `packages/morgendrot-core` — bekannt, **`TESTING.md`** Schritt **1**); **`cd frontend`**: **`npx tsc --noEmit`**, **`npm run lint`**, **`npm run check:circular`**, **`npm run test:unit`** (44 Dateien, 243 Tests); Root: **`npm run test:core`** (17/61), **`npm run test:h15-direct-submit`** (5 Tests `direct-iota-plain-submit.test.ts`), **`npm run validate:ui`**, **`npm run test:smoke`** (36/36) | **OK** (automatisiert). **`HANDY-FIRST` § 2 Schritte 1–5** (Browser: Puls Direkt/API, Klartext-Send, Basis stop, Moduswechsel) — **manuell** am laufenden **`npm run dev`** + entsperrtem Tresor; Abhaken in der Smoke-Doku beim Nutzer. |
| **2026-03-28** | Repo lokal (Windows), Attestation-Drain + **H.15**-Hybrid | **`cd frontend`**: **`npx tsc --noEmit`**; **`npx vitest run`** `attestation-manifest-anchor.test.ts`, `direct-iota-plain-submit.test.ts`; Root: **`npm run test:h15-direct-submit`**, **`npm run test:smoke`** | **OK** — `browserAttestationSubmit` / `[[MORG_ATTESTATION_MANIFEST_V1]]`; Mirror-Drain auch bei **nur** Attestation-Queue; SYNC § 8 ergänzt. |
| **2026-03-28** | Repo lokal (Windows), § **H.15** `mailbox-send-hybrid` (Composer, SOS, Spiegel, Mirror, Lora-Confirm, Anker) | **`cd frontend`**: **`npx tsc --noEmit`**; **`npx vitest run`** `direct-iota-plain-submit.test.ts`, `offline-queue.test.ts` | **OK** — zentraler Hybrid; **`use-chat-view-mirror-delay`** braucht **`mailboxRecipient`**. Merge: **`test:smoke`** / **`test:unit`** bei PR. |
| **2026-03-28** | Repo lokal (Windows), § **H.15** Live-Send hybrid + Header-Badge | **`cd frontend`**: **`npx vitest run frontend/lib/direct-iota-plain-submit.test.ts`** (5 Tests); Root: **`npm run test:h15-direct-submit`** | **OK** — `canTryLivePlaintextDirectMailbox`; Doku **`TESTING.md`** 5c, **`HANDY-FIRST-STAGE2-…`**, **`ROADMAP-FAHRPLAN`** Betriebsrhythmus. Merge-Ritual bei nächstem PR: **`test:smoke`** + **`test:unit`** nachziehen. |
| **2026-03-29** | Repo lokal (Windows), nach Messenger-PWA-/Puls-/Dashboard-UX | Root: **`npm run test:smoke`** (36/36); **`cd frontend`**: **`npx tsc --noEmit`**, **`npm run lint`**, **`npm run test:unit`** (41 Dateien, 232 Tests) | **OK** — § **H.0/H.1/H.2**-Scheibe (PWA **standalone** → **`/vault-lock`** bei Hintergrund; **`sessionStorage`** letzte Kachel; Posteingang **Telefonbuch**; Puls **Ketten-IDs**-Normalize); **`next.config`** **`allowedDevOrigins`** Host-Format (**`DEV-START`**). Feldtest: **`HANDY-TEST-WINDOW`**, **`PWA-MANUAL-CHECKS`**. |
| **2026-03-28** | Repo lokal (Windows), Fahrplan-Paket **§ H.15 → § H.2** (Reihenfolge) | Root: **`npm run test:h15-direct-submit`**, **`npm run test:smoke`**; **`npm run check:pwa-desk:full`** (A+B+C); **`cd frontend`**: **`npm run lint`**, **`npm run lint:feature-boundaries`**, **`npm run test:unit`** (41 Dateien, 232 Tests) | **OK** — H.15-Vitest (3), Smoke 36/36, Next **16.1.6** Production-Build; ESLint inkl. Feature-Grenzen; Vitest grün. **§ H.15 Stufe 2 § 2** (Browser/Testnet, Basis stop/start, Moduswechsel) bleibt **manuell** — **`docs/HANDY-FIRST-STAGE2-CLIENT-SUBMIT-SMOKE.md`**. |
| **2026-03-28** | Repo lokal (Windows), nach TS-Kleinfix | **`cd frontend`**: **`npx tsc --noEmit`**, **`npm run lint`**, **`npm run check:circular`**, **`npm run test:unit`** (40 Dateien, 226 Tests); Root: **`npm run test:core`**, **`npm run test:h15-direct-submit`**, **`npm run test:smoke`** | **OK** — Frontend-`tsc` grün; Lint + madge grün; Vitest/Core/H.15/Smoke grün. Root **`npx tsc --noEmit`** weiterhin TS6059 (`rootDir` nur `src/` vs. `packages/morgendrot-core`) — bekannt; Qualität über Frontend-`tsc` + Core-Tests. |
| **2026-03-28** | API **:3342**, Windows, `.env`; Status vor Connect bereits `connected=true` / `locked=false` | **`npm run test:smoke`** | **OK** — 36 bestanden, 0 fehlgeschlagen (`validate:ui` + `npm run test`). |
| **2026-03-28** | selbe, `SINGLE_WALLET=1` | **`npm run test:messages:single`** | **OK** — Abschnitte **1**–**7** vollständig (kompaktes Bild, Handshake/Connect, `/send`/`/fetch`, Filter, Klartext, `purge-handshake`, `/vault-save`, `hasLocal`; Hinweiszeile Ticket-Skript am Ende). |
| **2026-03-28** | selbe | **`npm run test:frontend-unit`** | **OK** — 37 Testdateien, 219 Tests, ~6 s. |
| **2026-03-28** | selbe | **`npm run test:core`** | **OK** — 14 Dateien, 51 Tests (`@morgendrot/core`). |
| **2026-03-28** | selbe | **`npm run test:h15-direct-submit`** | **OK** — 3 Tests `direct-iota-plain-submit.test.ts`. |
| **2026-04-28** | Repo lokal (Windows), API **3342** laut `.env`, Wallet **locked** | **`npm run test:smoke`** | **OK** — 36/36 Modulgruppen (`validate:ui` + `npm run test`). |
| **2026-04-28** | selbe | **`npm run test:frontend-unit`** | **OK** — Vitest 37 Dateien, 219 Tests. |
| **2026-04-28** | selbe | **`npm run test:core`** | **OK** — `@morgendrot/core` 14 Dateien, 51 Tests. |
| **2026-04-28** | selbe | **`npm run test:h15-direct-submit`** | **OK** — 3 Tests `direct-iota-plain-submit.test.ts`. |
| **2026-04-28** | selbe | **`npm run test:messages`** (Default, zwei APIs) | **Nicht ausgeführt** — `ECONNREFUSED :3343` (zweite Instanz fehlt). Hinweis im Skript: **`npm run test:messages:single`** oder `$env:SINGLE_WALLET='1'`. |
| **2026-04-28** | selbe | **`$env:SINGLE_WALLET='1'; npm run test:messages`** | **Teil OK** — Chain/help/connect-Checks; **Send/Handshake/Kompaktbild** ab **1d** mit `locked=true` fehlgeschlagen („Wallet entsperren“). **Vollständiger Lauf:** API starten, dann **UI-Unlock** oder **`UNLOCK_PASSWORD`** (siehe **`scripts/run-messages-chat-realworld.ts`** Kopfkommentar, **`.env.example`**). |
| **2026-04-28** | selbe | **`npm run test:realworld`** | **Abbruch erwartbar** — gleiche Ursache `locked=true` (Tickets/Keys brauchen entsperrte Session). |
| **2026-04-28** | API **:3342**, `locked=false` (UI-Unlock), `SINGLE_WALLET=1` | **`npm run test:messages:single`** | **OK** — vollständiger Ablauf laut Skript (Einrichtung **1**–**7**: kompaktes Bild `/send-plain`, Handshake/Connect, `/send`/`/fetch`, Filter, Klartext, `purge-handshake`-Noop ohne `MAILBOX_ID`, `/vault-save`, `hasLocal`). |
| **2026-04-28** | selbe, Wallet entsperrt | **`npm run test:realworld`** | **Teil OK** — Ticket (1) Mint OK; (2) **FAIL** — **Client/Server api version mismatch** (IOTA-CLI ≠ RPC-API); (4) `hasValidTicket` **false** (Folge). **Maßnahme:** CLI-Version an **`RPC_URL`**-Server anpassen (**`TESTING.md`** Smoke, Punkt 3). |

---

## Bekannte Störung: „Client/Server api version mismatch“ (nur Ticket-/AccessKey-Skript)

- **Symptom:** **`npm run test:tickets-accesskey-realworld`** (Alias **`test:realworld`**) bricht bei **personalisiertem Ticket** / weiteren PTB-Schritten ab; Node/SDK und **IOTA-CLI** erwarten dieselbe **Wire-/API-Version**.
- **Lösung:** Auf dem Rechner die **IOTA-CLI** installieren/aktualisieren, die zur **`.env`**-**`RPC_URL`** passt (`iota client …` / Release-Notes des Netzes); **Ticket-Skript** erneut ausführen.
- **Abgrenzung:** **`npm run test:messages:single`** = **Messenger** (primär **Node-API**) — kann **grün** sein, während der **Ticket-Lauf** rot ist (CLI-Pfad). Messenger-Qualität **nicht** an den Ticket-Lauf koppeln.

---

## Nächste Pflege

- Nach CLI-Angleichung: **`test:tickets-accesskey-realworld`** erneut und Zeile oben **OK** ergänzen (inkl. Commit-Hash optional).
- CI: **`.github/workflows/frontend-checks.yml`** spiegelt Frontend-Unit; Root-Smoke lokal oder in eigener Pipeline pflegen.

---

*Stand: 2026-03-28 — u. a. vollständiges Merge-Ritual + H.15-`test:h15-direct-submit` dokumentiert; Smoke + `test:messages:single` OK; Messenger/Ticket-Läufe getrennt.*
