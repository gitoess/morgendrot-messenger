# Rollen-Overlay — nur die Zeilen, die sich pro Profil unterscheiden.
# Anwenden: npm run env:role:consumer  (patcht die bestehende .env)
# Siehe docs/TEST-ROLLE-PROFILES.md

ROLE=messenger
DEPLOYMENT_PROFILE=consumer
UI_VARIANT=messenger
