# Rollen-Overlay — Consumer / Wanderer (privat, Mesh-First, Simple Mode)
# Anwenden: npm run env:role:consumer

ROLE=messenger
DEPLOYMENT_PROFILE=consumer
UI_VARIANT=messenger
TRANSPORT_PROFILE=mesh-first
SIMPLE_MODE=true
