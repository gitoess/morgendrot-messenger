# Rollen-Overlay — Arbeiter (Einsatz, Simple Mode, Mesh-First)
# BOSS_ADDRESS / KOMMANDANT_ADDRESSES in .env setzen, wenn Hierarchie-Tests nötig sind.

ROLE=arbeiter
DEPLOYMENT_PROFILE=einsatz
UI_VARIANT=messenger
TRANSPORT_PROFILE=mesh-first
SIMPLE_MODE=true
