/**
 * § H.25a — Flüchtig (LoRa): harte Grenzen (shared für attachments, send, UI).
 * Keine Abhängigkeit zu features/send — siehe MESSENGER-UI-MODULARITY-STRATEGY.
 */

export {
  FLUENT_LORA_JPEG_PAIR_TOTAL_MAX_BYTES as FLUENT_LORA_IMAGE_MAX_TOTAL_BYTES,
  FLUENT_LORA_MAX_SEGMENTS_PER_PHASE,
} from '@morgendrot/core/image'
