# `features/voice/`

Sprachmemo-Hilfen (Dauer-Limits, `MediaRecorder`-MIME) ohne React. **`lib/messenger-voice-record.ts`** re-exportiert für alte Pfade. Tests: **`messenger-voice-record.test.ts`**.

Siehe `features/README.md`.
