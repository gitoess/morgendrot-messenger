# `features/inbox/`

Reine Posteingangs-Hilfen (Partner/Richtung-Filter, Fehlertexte, …) ohne React. **`lib/inbox-*.ts`** re-exportiert für alte Pfade.

Siehe `features/README.md` und `docs/FRONTEND-API-MODULARITY.md`.
