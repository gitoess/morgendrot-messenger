# `features/inbox/`

Reine Posteingangs-Hilfen (Filter, Fehlertexte, …) ohne React. **`lib/inbox-*.ts`** kann weiterhin Re-Exports bereitstellen.

Siehe `features/README.md` und `docs/FRONTEND-API-MODULARITY.md`.
