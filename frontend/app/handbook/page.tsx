import { HandbookClient } from '@/components/handbook-client'

export const metadata = {
  title: 'Handbuch | Morgendrot',
  description: 'Boss-Orientierung und Offline-Hinweise (PWA, lokal gecacht)',
}

export default function HandbookPage() {
  return <HandbookClient />
}
